# React Example

## Required
 - nodejs v7

## Development

```
$ npm install yarn -g
$ yarn install
$ yarn run build -- --watch
$ PORT=5000 yarn start
$ yarn run bs -- --proxy localhost:5000 --files *
```
