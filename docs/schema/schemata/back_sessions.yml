---
"$schema": http://json-schema.org/draft-04/hyper-schema
title: 裏画面用セッション管理 - 内部向け/Sessions
description: セッションを管理します
stability: prototype
strictProperties: true
type:
  - object
links:
  - description: 接続中のセッション一覧を表示する
    href: "/v1/back/sessions"
    method: GET
    rel: instances
    http_header:
      Authorization: "Idaten {gspadmin session id}:{user account}"
    title: List
  - description: 接続中のセッションを切断する
    href: "/v1/back/sessions/{(%2Fschemata%2Fsessions%23%2Fdefinitions%2Fid)}"
    method: DELETE
    rel: destroy
    http_header:
      Authorization: "Idaten {gspadmin session id}:{user account}"
    title: Delete
properties:
  session:
    "$ref": "/schemata/sessions#/definitions/session"
id: schemata/back_sessions
